{
  "name": "medtodo",
  "version": "2.1.0",
  "private": true,
  "description": "Task manager for clinical, academic, and home work. No build step, no dependencies.",
  "scripts": {
    "test": "node test/app.test.js && node test/sync.test.js"
  }
}
