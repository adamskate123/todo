# Todo List (Obsidian Ready)

A lightweight browser-based todo list that stores tasks in `localStorage` and supports Markdown
checklists for Obsidian integration.

## Features

- Add tasks with notes, priority, tags, and due dates.
- Filter and search tasks.
- Export and import Markdown checklists for Obsidian.
- Runs as a static site (ideal for GitHub Pages).

## GitHub Pages

1. Push this repo to GitHub.
2. In **Settings → Pages**, select the branch (e.g. `main`) and root directory.
3. Save, then open the provided URL.

## Obsidian Integration

- Use **Export to Markdown** and paste into an Obsidian note.
- Paste any Obsidian checklist into the importer to sync tasks.
